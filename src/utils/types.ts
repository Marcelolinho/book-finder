

// A interface serve como um contrato, assim como no Java.
export interface Book {
    searchId: string,
    name: string,
    author: Author
    platform: string,
    thumbnail: string,
},

export interface Author {
    
}